import '@testing-library/jest-dom';
import { beforeAll, afterAll, beforeEach, afterEach } from 'vitest';

// Global hooks for testing setups
beforeAll(() => {
  // Global setup
});

afterAll(() => {
  // Global teardown
});

beforeEach(() => {
  // Ran before every test
});

afterEach(() => {
  // Ran after every test
});
